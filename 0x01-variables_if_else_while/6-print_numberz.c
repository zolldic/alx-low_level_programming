#include <stdio.h>

/**
*
* main - program that prints all single digit numbers starting from 0
*
* Return: Always (0) on success
*/

int main(void)
{

	int zero = 48;
	
	while (zero != 58)
	{
		putchar(zero);
		zero++;
	}

	putchar(10);

	return (0);
}
